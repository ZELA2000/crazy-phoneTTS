# crazy-phoneTTS - Esempio Configurazione Ambiente

# Copia questo file in .env e inserisci le tue credenziali

# Backend
BACKEND_HOST=0.0.0.0
BACKEND_PORT=8000

# Frontend (solo per override manuale)
# FRONTEND_API_URL=http://localhost:8000

# Servizi TTS (configura almeno uno)

# Azure Speech Services
AZURE_SPEECH_KEY=your-azure-speech-key-here
AZURE_SPEECH_REGION=italynorth
# AZURE_SPEECH_ENDPOINT=https://your-custom-endpoint.cognitiveservices.azure.com/

# Google Cloud TTS
# GOOGLE_APPLICATION_CREDENTIALS=/path/to/your/service-account-key.json

# Timeout e retry (Azure)
AZURE_CONNECTION_TIMEOUT=30
AZURE_REQUEST_TIMEOUT=60
AZURE_MAX_RETRIES=3
AZURE_RETRY_DELAY=2.0

# Configurazioni aziendali (proxy/firewall)
# AZURE_CONNECTION_TIMEOUT=60
# AZURE_REQUEST_TIMEOUT=120
# AZURE_MAX_RETRIES=5
# AZURE_RETRY_DELAY=3.0

# Guida rapida setup:
# 1. Azure: crea risorsa "Speech Services", copia chiave e region
# 2. Google: crea progetto, abilita API, crea Service Account, scarica JSON
# 3. Inserisci le variabili sopra, salva come .env
# 4. Avvia: docker-compose up --build
# 5. Test: http://localhost:3000 (web), http://localhost:8000/health (API)

# Troubleshooting:
# - Verifica path e permessi file credenziali
# - Riavvia Docker dopo modifiche
# - Consulta i log per errori di connessione
# - Per ambienti aziendali, aumenta timeout e verifica proxy/firewall